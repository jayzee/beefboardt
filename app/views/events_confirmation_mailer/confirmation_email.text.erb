Hi there. Your BeefBoardt event <%=@event.name%> is confirmed.

A quick reminder:
Where: <%=@event.location%>
Who: <%@event.attendees.each do |attendee| %>
   - <%= attendee.name_w_initial %> - 
   <% end %>
When: <%=@event.event_time.strftime('%A, %B %Cth @ %l:%M%p')%>
What: <%= @event.description %>

You host is <%=@user.name_w_initial%>. <% if @event.attendee_cost == nil %> Don't worry about payment, this event is FrEe! <% else %> You owe <%=@user.name_w_initial%> <%= number_to_currency(@event.attendee_cost)%>. <% if @user.phone %>Find <%=@user.first_name%> on venmo at <%=number_to_phone(@user.phone)%> <% end%> <% end %>  

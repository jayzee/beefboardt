
<div class="row">
        <h2><%= link_to_if event.name, event.name, event_path(event) %></h2>
          <div class="col-md-6">
          <ul>
            <li>location: <%= event.location if event.location%></li>
            <li>when: <%= event.event_time.strftime('%A, %B %Cth @ %l:%M%p') if event.event_time%></li>
            <li>description: <%= event.description if event.description%></li>
            <li>Per Person Cost: <%= event.cost_per_person if event.cost_per_person%></li>
            <li>Flat cost: <%= event.flat_cost if event.flat_cost%></li>
            <li>signup deadline: <%= event.signup_deadline.strftime('%A, %B %Cth @ %l:%M%p') if event.signup_deadline%></li>

            <% if event.tags %>
            <li>Tags: <ul><% event.tags.each do |tag| %>
              <li><%= link_to tag.name, controller: "events", tag: "#{tag.name.downcase}" %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
    </div>

    <div class="container">
      <h3>Is this event confirmed?</h3>
          <% if event.confirmed %>
          This event is confirmed and will be happening! The minimum attendance number has been hit.
          Look forward to seeing you there!
          <% else %>
            This event has not been confirmed. We are still waiting for the minimum amount of people to
            agree to attend.

          <% end %>
    </div>

<br><br>


    <div class="container">

          <% if @user && @user.is_the_user_a_host(@event) %>
              You are the host!
            <%= link_to "Edit the event", edit_event_path %>

          <% elsif @user && @user.is_the_user_an_attendee(@event) %>
            <p>You are planning on attending this event!</p>

          <% else %>

                <%= form_tag(attend_event_path, method: 'POST') do %>
                  <p><%= label_tag "Would you like to attend?" %>
                      <%= hidden_field_tag :user_id, @user.id if @user %>
                <%= submit_tag "Attend!" %></p>
                <% end %>
          <% end %>

  </div>

You're on the homepage! 
<%= form_tag(new_event_path, method: 'GET') do %>
  <p><%= label_tag "Create a new event:" %>
  <%= text_field_tag :name %></p>
  <p><%= submit_tag "Let's go" %></p>
<% end %>

<br><br>
<h2>List of Events</h2>

<%= render partial: "event", collection: @events %>

<%# Renders event info %>
<%= render @event %>

<%# show event host %>
Hosted by: <%= @event.host_name %>

<%# Lets user either attend event or edit if they are the host %>

  <% if @user && @user.is_the_user_a_host(@event) %>
      You are the host!
    <%= link_to "Edit the event", edit_event_path %>

  <% elsif @user && @user.is_the_user_an_attendee(@event) %>
    <p>You are planning on attending this event!</p>

  <% else %>

    <%= form_tag(attend_event_path, method: 'POST') do %>
      <p><%= label_tag "Would you like to attend?" %>
          <%= hidden_field_tag :user_id, @user.id if @user %>
    <%= submit_tag "Attend!" %></p>
    <% end %>
  <% end %>

  <%# shows attendees %>

  <ul><% @attendees.each do |attendee| %>
    <li><p><%= attendee.name_w_initial %></p></li>
  <% end %></ul>



